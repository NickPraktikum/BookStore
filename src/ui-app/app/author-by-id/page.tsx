// The entry page with the search form for a author's search by id.
import SearchForm from "../components/SearchForm";

export default function Deleted() {
  return <SearchForm mode="author" />;
}
